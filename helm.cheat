% zeno, helm

# Get helm list and describe (need plugin: fullstatus)
helm fullstatus <release>

# Get helm values by name
helm get values <release>

$ release : helm ls --- --header-lines 1 --column 1
